export default function SubmitReport() {
  // TODO: form (title, description, category, evidence, optional identity,
  // "submit anonymously" toggle) -> POST /api/reports
  // On success, prominently display the tracking ID (shown once).
  return <h2>Submit a Report</h2>;
}
