export default function ReviewerDashboard() {
  // TODO: GET /api/reports/assigned, list + open detail (decrypted server-side),
  // update status -> PATCH /api/reports/:id/status
  return <h2>Reviewer Dashboard</h2>;
}
