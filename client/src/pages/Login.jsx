export default function Login() {
  // TODO: form (username, password) -> POST /api/auth/login
  // -> if 2FA required, route to a code-entry step -> POST /api/auth/verify-2fa
  return <h2>Login</h2>;
}
